import { expect } from 'chai';
// import sinon from 'sinon';
// import ProductModel from '../../../src/database/models/product.model';
// import productMock from '../../mocks/product.mock';
// import productService from '../../../src/services/product.service';

// describe('ProductsService', function () {
//   beforeEach(function () { sinon.restore(); });

//   describe('Service Verifica Products', function () {

//     it('Service - Endpoint POST: Cadastrando um produto com sucesso', async function () {
//       const newProduct = productMock.newProduct;
//       const returnCreate = productMock.returnCreate;
//       const newProductReturn = ProductModel.build(productMock.postProduct);
//       sinon.stub(ProductModel, 'create').resolves(newProductReturn);
    
//       const serviceResponse = await productService.create(newProduct);
    
//       expect(serviceResponse.status).to.be.equal(201);
//       expect(serviceResponse.data).to.be.deep.equal(returnCreate);
//     });
//   });
// });
